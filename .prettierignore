node_modules/
dist/
.next/
coverage/
pnpm-lock.yaml

# Generated Prisma clients and the migrations Prisma authors — both are output,
# not source, and neither is ever hand-edited.
apps/*/generated/
apps/*/prisma/migrations/

# Generated by `next build` on every run — formatting it is a losing race.
**/next-env.d.ts

# Design source, kept verbatim as authored.
design/

# A verbatim transcription of the assignment. It is a record, not our prose --
# reformatting it would blur the line between what was issued and what we wrote.
docs/BRIEF.md

# Prettier is not idempotent on this file: --write collapses its nested bullet
# lists into inline text and then indent-creeps on every subsequent run. It is a
# long structured working document, not shipped prose, so it is exempt like
# BRIEF.md above.
docs/PLAN.md
