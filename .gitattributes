# Built on Windows, reviewed on Linux/macOS: normalize to LF in the repo so the
# diff never shows phantom line-ending churn.
* text=auto eol=lf

# Binary / generated — never diffed, never normalized.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
pnpm-lock.yaml linguist-generated=true -diff
